name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go vet ./...
      - run: go test ./...
      # Rerun the perf smoke and spawn budget verbosely so the timing
      # numbers and spawn counts are visible in the CI log (TestNoOpPerf
      # is advisory in CI; TestSpawnBudget gates everywhere).
      - if: runner.os == 'Linux'
        run: go test ./internal/snap -run 'TestNoOpPerf|TestSpawnBudget' -v -count=1
      - run: go build ./...
      # The installers can't run in CI, but they can at least parse.
      - if: runner.os == 'Linux'
        run: sh -n install.sh
      - if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $errs = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile("$PWD/install.ps1", [ref]$null, [ref]$errs)
          if ($errs) { $errs; exit 1 }
