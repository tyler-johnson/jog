jog shell list — every supported shell and what is installed

usage:
  jog shell list

One row per supported shell: its rc file, whether that file exists, and
whether jog's alias line (or a hand-written one) is present. The names
it prints are the ones install and uninstall accept.
